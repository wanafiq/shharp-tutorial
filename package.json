{
    "name": "sharp-tutorial",
    "version": "1.0.0",
    "main": "dist/index.js",
    "license": "MIT",
    "scripts": {
        "clean": "rimraf dist",
        "format": "prettier --write .",
        "build": "tsc",
        "start": "node dist/index.js",
        "dev": "yarn clean && yarn format && yarn build && yarn start",
        "parseExcel": "yarn clean && yarn format && yarn build && node dist/services/dataService.js && yarn prettier --write assets/data/quotes.json"
    },
    "devDependencies": {
        "@types/sharp": "^0.28.3",
        "prettier": "^2.3.2",
        "tslint": "^6.1.3",
        "typescript": "^4.3.5"
    },
    "dependencies": {
        "rimraf": "^3.0.2",
        "sharp": "^0.28.3",
        "xlsx": "^0.17.0"
    }
}
